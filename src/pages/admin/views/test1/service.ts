import { CommonService } from "@/services";

export default class Service extends CommonService {
    // public async page(queryModel: any): Promise<any> {
    //     return this._post(`${this.authingServer}/unity/login-log/page`, queryModel);
    // }
}
